<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/css/style.css' />
  </head>
  <body>
    <div>
      <a class="<%= idx===1 ? "on" : "" %>" href='/'>首页</a>
      <a class="<%= idx===2 ? "on" : "" %>" href='/news'>新闻</a>
      <a class="<%= idx===3 ? "on" : "" %>" href='/about'>我的</a>
    </div>
